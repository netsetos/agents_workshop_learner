# GENERATED by make_modelfile.py from the tokenizer. Do not hand-edit.
# Regenerate whenever the checkpoint changes - the template belongs to the
# weights, not to the project.
#
# This copy is a PLACEHOLDER committed so the Dockerfile builds in CI. The real
# one is produced during 11.4 from the checkpoint 10.5 exported:
#
#   python make_modelfile.py --model ./documind-slm > Modelfile
#
# It is deliberately not a hand-typed Gemma template. The previous version of
# this file carried Gemma 3's <start_of_turn> tokens while 10.5 trains
# gemma-4-E2B-it, which is exactly the mismatch the generator prevents.

FROM ./documind-slm.gguf

TEMPLATE """{{ .Prompt }}"""

PARAMETER num_ctx 4096

SYSTEM """You are DocuMind. Answer only from the documents provided.
End every answer with a citation in the form [doc:chunk_id]."""
